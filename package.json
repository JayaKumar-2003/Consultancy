{
  "name": "shop-management",
  "version": "1.0.0",
  "description": "Jayakumr",
  "main": "index.js",
  "scripts": {
    "build":"cd frontend && npm install &&  npm install && npm run build",
    "start": "cd backend && npm install && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
    
  },
  "author": "",
  "license": "ISC"
}
